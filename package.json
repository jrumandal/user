{
  "name": "@mf/user",
  "version": "0.0.1",
  "private": false,
  "type": "module",
  "description": "User micro-frontend (Vue 3) — source-only ESM package consumed by the shell.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "node -e \"console.log('@mf/user is source-only (no build step).')\"",
    "publish": "node scripts/publish.mjs"
  },
  "dependencies": {
    "@shared/contracts": "workspace:*",
    "@shared/design-tokens": "workspace:*",
    "@shared/event-bus": "workspace:*",
    "tslib": "^2.3.0"
  },
  "peerDependencies": {
    "vue": "^3.5.0",
    "@vue/server-renderer": "^3.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/node": "^22.10.0",
    "@vue/server-renderer": "^3.5.0",
    "@vue/test-utils": "^2.4.6",
    "eslint": "9.39.5",
    "jsdom": "^26.1.0",
    "typescript": "5.9.3",
    "typescript-eslint": "^8.35.0",
    "vitest": "^3.2.0",
    "vue": "^3.5.0"
  }
}
